volumes:
  ckan_storage:
  pg_data:
  solr_data:
  pip_cache:
  site_packages:
  local_bin:
  home_dir:

services:
  ckan-dev:
    platform: linux/amd64
    build:
      context: ckan/
      dockerfile: Dockerfile.dev
      args:
        - TZ=${TZ}
    env_file:
      - .env
    links:
      - db
      - solr
      - redis
    ports:
      - "0.0.0.0:${CKAN_PORT_HOST}:5000"
    volumes:
      - ckan_storage:/var/lib/ckan
      - ./src:/srv/app/src_extensions
      - pip_cache:/root/.cache/pip
      - site_packages:/usr/local/lib/python3.14/site-packages
      - local_bin:/usr/local/bin
      - home_dir:/srv/app/
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000"]
      interval: 60s
      timeout: 10s
      retries: 3

  docs:
    image: node:22-alpine
    command: >
      sh -c "apk add --no-cache git &&
        corepack enable &&
        corepack prepare pnpm@latest --activate &&
        if [ -d /ckanaction-pnaig/.git ]; then
          git -C /ckanaction-pnaig fetch --depth 1 origin pnaig_remote &&
          git -C /ckanaction-pnaig checkout -B pnaig_remote origin/pnaig_remote;
        else
          rm -rf /ckanaction-pnaig &&
          git clone --depth 1 --branch pnaig_remote https://github.com/otic08/ckanaction-pnaig.git /ckanaction-pnaig;
        fi &&
        cd /ckanaction-pnaig/docs &&
        pnpm install --frozen-lockfile &&
        pnpx ts-node ./scripts/generate-docs.ts &&
        pnpm dev"
    environment:
      - NEXT_TELEMETRY_DISABLED=1
    ports:
      - "0.0.0.0:${DOCS_PORT_HOST:-3000}:3000"
    depends_on:
      ckan-dev:
        condition: service_healthy
    restart: unless-stopped

  datapusher:
    image: ckan/ckan-base-datapusher:${DATAPUSHER_VERSION}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://localhost:8800"]
      interval: 60s
      timeout: 10s
      retries: 3

  db:
    build:
      context: postgresql/
    environment:
      - POSTGRES_USER
      - POSTGRES_PASSWORD
      - POSTGRES_DB
      - CKAN_DB_USER
      - CKAN_DB_PASSWORD
      - CKAN_DB
      - DATASTORE_READONLY_USER
      - DATASTORE_READONLY_PASSWORD
      - DATASTORE_DB
    ports:
      - "0.0.0.0:5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test:
        ["CMD", "pg_isready", "-U", "${POSTGRES_USER}", "-d", "${POSTGRES_DB}"]

  solr:
    image: ckan/ckan-solr:${SOLR_IMAGE_VERSION}
    volumes:
      - solr_data:/var/solr
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO", "/dev/null", "http://localhost:8983/solr/"]

  email-worker:
    platform: linux/amd64
    build:
      context: ckan/
      dockerfile: Dockerfile.dev
      args:
        - TZ=${TZ}
    env_file:
      - .env
    entrypoint:
      ["ckan", "-c", "/srv/app/ckan.ini", "jobs", "worker", "pnaig-emailer"]
    links:
      - db
      - solr
      - redis
    volumes:
      - ckan_storage:/var/lib/ckan
      - ./src:/srv/app/src_extensions
      - pip_cache:/root/.cache/pip
      - site_packages:/usr/local/lib/python3.14/site-packages
      - local_bin:/usr/local/bin
      - home_dir:/srv/app/
    depends_on:
      ckan-dev:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:${REDIS_VERSION}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-e", "QUIT"]
